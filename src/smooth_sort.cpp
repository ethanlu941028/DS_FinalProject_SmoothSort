#include "smooth_sort.h"

// The implementation is templated in the header. This source file exists so
// the project can be extended with explicit instantiations in the future if
// desired. Currently no additional definitions needed.
